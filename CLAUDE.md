# CLAUDE.md

`PlusMessage.backup`（ドコモ/au/ソフトバンクの ＋メッセージ）をリバース
エンジニアリングして閲覧するビューアー。解析ノートは [docs/](./docs/) に、
未解決の疑問は [docs/open-questions.md](./docs/open-questions.md) にある。

## 個人情報を書かない（最優先）

**このリポジトリは公開前提。解析対象は実在する個人のバックアップなので、
そこから読み取った値をコミットしてはいけない。** docs / コード / コメント /
テスト / コミットメッセージ、すべてに適用される。

### 絶対に書かないもの

- 実在する電話番号（`+81…`、`090-…` のどの表記でも）
- 実在する氏名（漢字・かな・ローマ字のいずれも）
- **実際のメッセージ本文**。全文でも断片でも、grep 用のフレーズでも
- UUID、端末 ID、アカウント ID、資材 URL のリソース ID
- 実画面のスクリーンショット

### 代わりに使う架空の値

| 種別             | 使う値                                         |
| ---------------- | ---------------------------------------------- |
| 電話番号（携帯） | `+81901234000N`、`+819011112222`               |
| 電話番号（固定） | `+8131234000N`                                 |
| 氏名             | 花子 / 山田太郎                                |
| メッセージ本文   | 架空の文面（日本語・絵文字・改行を含めてよい） |
| UUID             | 同じ書式の架空値                               |

### 書いてよいもの

バイトオフセット、セクションサイズ、件数、タイムスタンプ、
キャリアのインフラ由来の固定値（`operator@kw.ncs.spmode.ne.jp`、
`a-wss.kw.ncs.spmode.ne.jp` など）。仕様解読の根拠そのものであり、
単体では個人を特定しない。

### hexdump を載せるときの注意

**16 進数のバイト列は本文そのもの。** 読める側の文字列だけ架空にしても、
バイト列をデコードすれば実本文が読める。架空の文面から
**バイト列を再生成**し、記載のバイト長と一致することを確認すること。

```
e3 81 a9 e3 81 86 e3 82 82 f0 9f 99 8f 0a          "どうも🙏\n"
```

### コミット前チェック

```bash
# 架空の番号帯 (901234000…, 9012345678, 900000000, 9011112222, 31234000…) と
# 合成 UUID は除外して洗い出す
git diff --cached | grep -nE "\+81[0-9]{9,}|[0-9a-f]{8}-[0-9a-f]{4}-" \
  | grep -viE "90123400|9012345678|900000000|9011112222|31234000|11111111|22222222|3f2a91c7|7c8d9e0f|2b4d6f80"
```

ヒットしたら、それが実データでないことを確認してからコミットする。

`PlusMessage.backup` と検証用スナップショット / スクリーンショットは
`.gitignore` 済み。**除外設定を外さないこと。**

そのため **docs から未追跡ファイルを参照しない**。スクショは撮っても
コミットできないので、`スクリーンショット: foo.png` と書くと clone した人に
とっては存在しないファイルへのリンクになる。画面で何が見えたかは文章で書く。

## 開発

```bash
pnpm dev          # http://localhost:5173
pnpm test         # vitest
pnpm typecheck
pnpm exec eslint src test
pnpm format:check # prettier。崩れていたら pnpm format
pnpm tsx scripts/<name>.ts ./PlusMessage.backup   # 実ファイルでの検証
```

- パッケージマネージャは pnpm
- ブラウザでの動作確認は playwright-cli
- 実ファイルを触る検証スクリプトは `test/` ではなく `scripts/` に置く
- 新しい知見は `docs/findings-YYYY-MM-DD.md` に、使ったスクリプト・観察された
  出力・そこから何を結論したかをセットで残す

## 解析の姿勢

- **観測と仮説を区別する。** 「ファイル上で実際に観測した」ことと
  「こう解釈している」ことは別々に書く
- **オフセットは絶対値で書く。** 相対オフセットはスライスを跨ぐと壊れる
- **ドキュメントの記述を鵜呑みにしない。** 過去の推定が外れていた例が複数ある
  （0x0005 は「スレッド」ではなくメディア 1 件だった、など）
